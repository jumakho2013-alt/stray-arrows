
# Android signing config — passwords live here, never in build.gradle.
# Claude Code transient state — keep launch.json tracked, ignore everything else
# Use android/keystore.properties.example as a template for setup.
# Xcode user data (per-user state, not needed in repo)
# but log bundles can land in the project dir)
# iOS build artifacts (xcodebuild creates these in DerivedData by default,
# macOS metadata
*.aab
*.apk
*.keystore
*.xcresult
.DS_Store
.claude/scheduled_tasks.lock
.claude/sessions/
.claude/state/
.claude/worktrees/
android/.gradle/
android/app/build/
android/build/
android/capacitor-cordova-android-plugins/build/
android/keystore.properties
ios/App/App.xcodeproj/project.xcworkspace/xcuserdata/
ios/App/App.xcodeproj/xcuserdata/
ios/App/DerivedData/
ios/App/Pods/
ios/App/build/
node_modules/
tools/data/
